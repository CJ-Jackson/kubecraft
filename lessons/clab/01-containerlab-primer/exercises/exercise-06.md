1. I use `containerlab inspect -t topology/lab.clab.yml` to diagnose the problem

2. I fixed it with `docker start clab-first-lab-srl2`

3. `docker ps` only shows running containers, to see all add `-a`
